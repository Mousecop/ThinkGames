const initalState = {
    messages: [],
    currentUser: null,
    onlineUsers: []
}

export default (state = initalState, action) => {
    switch(action.type) {
        default:
            return state
    }
}